info:
  app:
    name: Saga Server
    description: Saga Server
spring:
  application:
    name: saga-server
  cloud:
    consul: # consul enabled configuration must be in this file
      enabled: false
    zookeeper:
      enabled: false
    nacos:
      discovery:
        enabled: false
